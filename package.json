{
  "name": "@renan-alm/js-action-demo",
  "version": "0.4.0",
  "description": "This is a demo of a JavaScript Action",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/index.js -o dist --source-map"
  },
  "keywords": [],
  "author": "Renan Almeida",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
